@model IEnumerable<DB_ProjectTRY.Models.category>

@{
    ViewBag.Title = "show";
}

<h2>show</h2>

<p>
    @Html.ActionLink("Create New", "add")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cat_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cat_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cat_image)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cat_fk_ad_id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cat_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cat_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cat_image)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cat_fk_ad_id)
        </td>
        <td>
            @Html.ActionLink("Edit", "update", new {id=item.cat_id }) |
            @*@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |*@
            @Html.ActionLink("Delete", "delete", new { id = item.cat_id })
        </td>
    </tr>
}

</table>
