@model IEnumerable<DB_ProjectTRY.Models.image>

@{
    ViewBag.Title = "show";
}

<h2>show</h2>

<p>
    @Html.ActionLink("Create New", "add")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.img_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.img_path)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.img_fk_prd_id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.img_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.img_path)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.img_fk_prd_id)
        </td>
        <td>
            @Html.ActionLink("Edit", "update", new { id=item.img_id }) |
            @Html.ActionLink("Delete", "delete", new { id = item.img_id })
        </td>
    </tr>
}

</table>
